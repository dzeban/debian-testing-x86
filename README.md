Docker image with debian testing 32-bit
=======================================

Based on rootfs created with `debootstrap` (minbase variant). 

With `build.sh` you can create the whole image by yourself.
